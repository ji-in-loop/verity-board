{
  "name": "@verity-board/orchestrator",
  "version": "0.1.0",
  "description": "The Chair: verity-board's execution engine — evidence planning, parallel actor reviews, stopping conditions.",
  "license": "MIT",
  "author": "Balajikumar Murugan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ji-in-loop/verity-board.git",
    "directory": "packages/orchestrator"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:coverage": "vitest run --coverage --coverage.reporter=lcov --coverage.reporter=text-summary"
  },
  "dependencies": {
    "@verity-board/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@verity-board/providers-model-mock": "workspace:*",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
